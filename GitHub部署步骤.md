# 部署到 GitHub Pages（路线 1：借一次能访问 github.com 的网络）

整个过程大约 10 分钟，只需要做一次。

## 0. 先把网络准备好

- 想办法让电脑能打开 **github.com**：手机开热点给电脑、开一下 VPN、或者换一条宽带都行。
- 验证方法：浏览器打开 <https://github.com/>，能看到登录页就 OK。
- 部署完就可以关掉，**手机以后访问不需要 VPN**（实测 github.io 在你这条网络下 72ms）。
- 如果你开了 VPN，而且已经登录了 GitHub，可以叫我直接帮你在浏览器里点（见文末）。

## 1. 建仓库（约 1 分钟）

1. 打开 <https://github.com/new>
2. **Repository name** 填 `timetable`（英文，别用中文）
3. 选 **Public**（GitHub Pages 免费版需要公开仓库）
4. 下面的 "Add a README file" 之类的初始化选项**都不要勾**
5. 点 **Create repository**

## 2. 上传文件（约 2 分钟）

### 方式 A：网页拖拽（推荐，不用装任何东西）

1. 在新仓库页面点 **uploading an existing file**（或 Add file → Upload files）
2. 打开电脑上的 `我的课表` 文件夹，**全选里面的文件**（Ctrl+A），拖进网页的虚线框
   - ⚠️ 要拖文件夹**里面的文件**，不要把 `我的课表` 这个文件夹本身拖进去，否则网址会变成 `…/我的课表/`
   - `.nojekyll` 有时拖不进去，不影响，跳过即可
3. 等文件列表出现（一共 17 个文件），下面写一句提交说明，比如 `第一版课表`，点 **Commit changes**

### 方式 B：用 Git 命令（你电脑现在没装 git，装了才用这条）

```bash
cd "你的路径\我的课表"
git init
git add .
git commit -m "第一版课表"
git branch -M main
git remote add origin https://github.com/你的用户名/仓库名.git
git push -u origin main
```

## 3. 打开 Pages（约 1 分钟）

1. 仓库页面 → **Settings**
2. 左侧菜单 → **Pages**
3. **Source** 选 `Deploy from a branch`；**Branch** 选 `main`，目录选 `/ (root)` → **Save**
4. 等 1–2 分钟，页面顶部会出现一行 `Your site is live at https://用户名.github.io/仓库名/`

## 4. 验证

- 电脑浏览器打开那个网址，应该看到课表
- **把网址发我**，我替你检查：manifest、图标、离线缓存、深色模式、能不能装到手机
- 常见坑：
  - 打开是 404 → 多半是还没部署完（再等两分钟），或者分支没选 `main`
  - 网址里多了 `我的课表/` → 说明把文件夹套了一层上传，重新按方式 A 传一次文件
  - 页面能开但没样式 → 刷新一下

## 5. 装到手机

1. 手机 **Chrome** 打开 `https://用户名.github.io/仓库名/`
2. 如果菜单里没有"安装"：**设置 → 应用管理 → Chrome → 权限 → 打开「创建桌面快捷方式」**，然后重开 Chrome
3. Chrome 右上角 `⋮` → **安装应用**
4. 验收清单见 `手机安装指南.md`

## 6. 以后要更新怎么办

- **改课表内容**：不用重新部署。课表在 App 里直接加/删/改，数据存在你手机本地。
- **我改了程序**（比如加课前提醒）：需要重新上传一次 —— 仓库 → Add file → Upload files → 拖新文件 → Commit，GitHub Pages 会自动重新部署（约 1 分钟）。
  这一步同样需要那一刻能访问 github.com，所以建议攒几次改动一起传。

## ⚠️ 隐私提醒

部署后这个仓库是**公开**的，任何人拿到网址都能看到：

- 你的课表内容（课名、老师、教室、周次）
- App 里显示的名字（"金皓天" / 问候语里的"皓天"）
- `README.md` 里的课表清单和课程列表

**不会**暴露：学号、密码、手机号（这些我没有放进程序；原始 PDF 里的学号只在我读 PDF 时用了一次）。

如果想去掉个人标识，跟我说一句，我把 App 里的称呼改成"同学"、README 里的课表清单删掉，再生成一份新的上传包（30 秒就能改好）。

## 我可以怎么帮你点

如果你电脑开了 VPN，并且**已经登录了 GitHub**，我可以直接在你的浏览器里操作：建仓库 → 打开上传页 → 帮你把文件选上（我这边能调起文件选择框）→ 你只需要在需要输密码、验证码、2FA 的时候接管一下。这样你就不用手动拖 16 个文件。

要做的话告诉我"你来点"，并把网络准备好、GitHub 登录好。
