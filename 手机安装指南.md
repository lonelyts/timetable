# 手机安装指南（OPPO Find X9 · ColorOS 16）

## 先说清楚分工

我没法远程操作你的手机、也看不到你的屏幕，所以流程是这样：

- **我准备**：所有文件、一个一键脚本、每一步该点哪里的清单；
- **你操作**：在电脑和手机上照着点；
- **卡住时**：把现象（或截图）发我，我判断是配置问题还是操作问题，改好再让你重试；
- **装完**：我给你一份验收清单，逐条确认真的是"像 App 一样"能用。

## 路线 A：今晚就能在手机上看到（局域网，不用注册任何账号）

1. 电脑和手机连**同一个 WiFi**。
2. 双击文件夹里的 **`局域网访问.bat`**。
3. 窗口里会打印一行地址，形如 `http://10.x.x.x:8081`（8080 被占用时它会自动换端口）。
4. 第一次运行如果 Windows 弹防火墙提示 → 点 **允许访问**。
5. 手机 Chrome 打开这一行地址 → 就能看到课表，能左右翻周、能点格子加课。

限制：电脑关机/关掉黑窗口就打不开；这种写法**装不了桌面图标**（要图标走路线 B）。
临时用的话，在手机 Chrome 里长按地址栏收藏一下，下次好找。

## 路线 B：装成桌面 App（推荐，这样才是"我的手机 App"）

原理：给它一个 https 网址，然后让 Chrome 把它"安装"到桌面——之后点图标就是全屏、没地址栏、**断网也能打开**，数据存在手机本地。

### ⚠️ 先看这条：GitHub 部署需要能访问 github.com

在你电脑现在的网络下实测（浏览器真实访问，2026-09-23）：

| 域名 | 用途 | 结果 |
| --- | --- | --- |
| `github.io` | **手机打开 App 用** | ✅ 72ms～1s，很快 |
| `github.com` | **登录、建仓库、上传文件用** | ❌ 20 秒超时，打不开 |
| `raw.githubusercontent.com` | GitHub 的原始文件 | ❌ 超时 |
| `api.github.com` | GitHub 接口 | ✅ 982ms |
| `netlify.app` | Netlify 站点 | ✅ 0.5～2.4s |
| `app.netlify.com` | Netlify 部署后台 | ❌ 8 秒超时（不确定是不是反爬） |
| `pages.dev`（Cloudflare） | 站点 | ❌ 超时 |
| `vercel.app` | 站点 | ❌ 超时 |
| `gitee.com` | 码云 | ✅ 196ms，飞快 |
| `cloud.tencent.com` / 控制台 | 腾讯云 | ✅ 0.15s / 6.3s（慢但能开） |

结论：

- **装好之后，手机打开 GitHub Pages 上的 App 不需要 VPN**（github.io 很快）；
- **但"部署"那一步需要能打开 github.com**，现在的网络打不开 → 这一步要借一次能访问 github.com 的网络（手机热点 / VPN / 换条宽带），10 分钟做完就行；
- 以后我改了程序要重新部署时，同样需要那一刻能访问 github.com；
- 只想改课表的话**不需要重新部署**：课表在 App 里直接加删改，数据存在手机本地。

如果不想为了部署折腾网络，走下面的"国内托管"或者路线 A / C。

### 国内托管（不需要 VPN，可选）

- **腾讯云 云开发静态托管**：控制台能打开，有免费额度，默认域名不用备案，国内访问快。缺点是注册 + 实名认证。
- **Gitee Pages**：gitee.com 很快，但 Pages 服务需要实名认证，免费额度政策这两年变过，要用的话我先帮你确认当前规则。

### B1. Netlify Drop（最简单，约 5 分钟）

1. 电脑浏览器打开 <https://app.netlify.com/drop>
2. 把整个 **`我的课表` 文件夹**拖进去（需要的文件都在里面了）
3. 等几秒，会得到 `https://xxx.netlify.app` 这样的网址
4. 用邮箱注册/登录一个免费账号（不登录的话这个网址过一阵子会被删掉）
5. **把网址发我**，我先替你验一遍：能不能装、图标对不对、离线缓存有没有生效

### B2. GitHub Pages（如果你有 GitHub 账号）

1. 新建一个仓库（Public 即可）
2. 把文件夹里的文件上传到仓库根目录
3. Settings → Pages → Source 选 `main` 分支 `/ (root)` → Save
4. 等 1 分钟，得到 `https://你的用户名.github.io/仓库名/`

### B3. Vercel / Cloudflare Pages

同一类操作，需要账号。要走这条我给你逐屏的操作顺序。

### 有了网址之后（三条路线都一样）

1. 手机用 **Chrome** 打开那个 https 网址（ColorOS 自带浏览器也能装，但 Chrome 最稳）。
2. 如果菜单里找不到"安装"，先开权限：
   **设置 → 应用管理 → Chrome → 权限 → 打开「创建桌面快捷方式」**（这是 ColorOS 特有的坑），然后重开 Chrome。
3. Chrome 右上角 `⋮` → **安装应用**（有的版本叫「添加到主屏幕」）。
4. 桌面出现橙粉渐变的日历图标 → 点开就是全屏 App。

## 装完的验收清单

- [ ] 桌面图标是橙粉渐变日历
- [ ] 点开是全屏，没有地址栏和浏览器底栏
- [ ] 顶部状态栏颜色和 App 底色一致（浅色奶油 / 深色棕黑）
- [ ] 开飞行模式也能打开，课表还在，能左右翻周
- [ ] 加一节课 → 完全退出 App → 重新打开 → 那节课还在
- [ ] 设置里切深色模式，状态栏跟着变深

## 我需要你先告诉我三件事

1. 电脑和手机是不是同一个 WiFi？（决定路线 A 能不能用）
2. 有没有 GitHub 账号？（没有就推荐 Netlify Drop）
3. 你手机上平时用 Chrome 还是自带浏览器？

## 我不会做的事

- 不会向你要账号密码或验证码：需要登录的步骤你自己操作，我只告诉你点哪里。
- 不会把你的课表传到任何地方：App 的数据只存在你自己设备的浏览器里（除非你自己部署时把文件传上去，那时传的也只是程序，不含你的课表数据）。
